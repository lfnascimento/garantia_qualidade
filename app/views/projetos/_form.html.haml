= semantic_form_for @projeto do |f|
  = f.inputs do
    = f.label :nome, "Nome"
    = f.input :nome
    = f.label :desc, "Descrição"
    = f.input :desc
    = f.label :responsavel, "Responsável"
    = f.input :responsavel

  = f.actions do
    = f.action :submit, :as => :input
